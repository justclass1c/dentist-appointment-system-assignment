#ifndef VERIFICATION_H
#define VERIFICATION_H

#include <random>
#include <string>

using namespace std;

bool verifyAge(string input);
bool verifyGender(char input);
bool verifyNRIC(string input);
bool verifyEmail(string input);
bool verifyPhoneNo(string input);

#endif
