#include <regex>
#include <string>
#include "../headers/Patient.h"

using namespace std;

template<typename T>;

bool validateEmail(vector<T> users, string input) {
    for (const auto& user : users) {

    }
    return false;
}
